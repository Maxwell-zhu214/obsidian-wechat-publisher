# Obsidian WeChat Publisher 开发与版本演进日志 (Dev Log)

该文档用于记录 `Obsidian WeChat Publisher` 插件的诞生、每一次重大技术升级、版本迭代以及后续的开发规划。

---

## 📅 版本迭代历史

### v1.1.0 (2026-06-01) - 核心排版与体验升级
*   **开发者署名与开源归属**：正式更名为 `Obsidian WeChat Publisher`，并将开发者署名归属于 **铭泽律师 (Maxwell)**，增加了设置界面的开源声明介绍。
*   **外链自动转脚注**：开发了 `convertLinksToFootnotes` 渲染逻辑。自动将正文中被微信禁用的外部超链接转换为文末的脚注列表，维持高水准的学术/专业阅读观感。
*   **全版式代码块与表格美化**：
    *   为「极细线框风」、「现代左竖风」、「商务色块风」三种排版模型分别定制了表格（`table`, `th`, `td`）与代码块（`pre`, `code`）的专属行内样式。
    *   解决了 Obsidian 渲染代码块在微信后台样式缺失、折行混乱的问题。
*   **YAML 封面图支持**：支持直接在 Obsidian 文章头部的 YAML Frontmatter 中使用 `cover` 字段指定封面图片路径。增强了封面配置的灵活性，实现了零干预的草稿配图。
*   **同步进度提示**：在上传图片时引入了 `[图片 i/N]` 的动态 Notice 气泡通知，优化大篇幅、多图文章的同步等待体验。

### v1.0.0 (2026-06-01) - 插件诞生与架构奠基
*   **绕过 Electron CORS 限制**：废弃了传统的浏览器端 `fetch`，使用 Node.js 原生 `https` 请求与手动构建多部分表单缓冲器（Multipart Buffer），解决了跨域安全策略阻碍图片和接口请求的问题。
*   **Obsidian 本地资源解析**：解决了 Obsidian 专属语法 `![[image.png]]` 转换为 `<span class="internal-embed">` 的标签解析难题，将其重构为原生 `<img>` 并通过 `vault.getResourcePath` 映射本地服务器 URL 渲染。
*   **排版容错过滤器**：
    *   实现了 `sanitizeListsForWeChat` 逻辑，剥离 `<li>` 标签内部嵌套的 `<p>` 标签，防止微信编辑器将其误解析为多余空行与多重序号。
    *   引入 `cleanEmptyParagraphs` 清理冗余的空白换行段落。
*   **三套经典版式**：内置「极细线框风」、「现代左竖风」、「商务色块风」三种视觉骨架，支持实时取色器（Hex Color Picker）预览。

---

## 🚀 未来升级规划与设想 (Roadmap)
1.  **图片哈希增量同步**：在本地存储已上传图片的 MD5 指纹，检测到无改动的图片直接引用微信 CDN 链接，避免每次同步重复上传，极速提升二次同步效率。
2.  **音视频多媒体支持**：解析音频与视频卡片，实现多媒体元素的自动化微信同步。
3.  **多帐号切换支持**：在配置中支持多套 AppID 与 AppSecret 配置，方便自媒体矩阵与法律团队矩阵的切换管理。
